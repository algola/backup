@using PapiroMVC.Validation
@model PapiroMVC.Models.ProductPartBookSheet


    @Html.AlgolaDisplayFor(model => model.Pages)

    @Html.AlgolaDisplayFor(model => model.Pages)
    <!-- artilce -->
    @for (int i = 0; i < Model.ProductPartsPrintableArticlePerView.Count; i++)
    {
        ViewBag.IndexPartArticle = i;

        @Html.DisplayFor(model => model.ProductPartsPrintableArticlePerView[i])           
    }

    <!-- task -->
    @for (int i = 0; i < Model.ProductPartTasksPerView.Count; i++)
    {
        @Html.DisplayFor(model => model.ProductPartTasksPerView[i])           
    }
